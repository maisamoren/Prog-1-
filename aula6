#include <stdio.h>

int fil = 12, pol = 4;
int assento, i, j, opcao;
char onibus [12][4];


// verifica o assento do onibus
int verificarAssento(char onibus[fil][pol], int assento){

	printf ("Digite o numero do assento: \n");
	scanf ("%d", &assento);

	return 0;
}


// reserva o assento do onibus
int reservarAssento(char onibus[fil][pol], int assento){
	return 0;
}


void imprimirMapa (char onibus[fil][pol], int fil, int pol){
	int i, j;
	
	for (i = 0; i < fil; i++){
		for (j = 0; j < pol; j++){
			printf ("%d\t", onibus[fil][pol]);
		}
		printf ("\n");
	}
}


// PROGRAMA PRINCIPAL *****************************************
int main () {

// preencher os assentos
// 	for (i = 0; i < fil; i++){
//		for (j = 0; j < pol; j++){
//			onibus[i][j] = '0';
//		}
//	}

	printf ("Digite 1 para verificar se um dado assento está ocupado ou livre \n");
	printf ("Digite 2 para reservar um assento livre \n");
	printf ("Digite 3 para imprimir o mapa de assentos \n");
	scanf ("%d", &opcao);


	if (opcao == 1){
		verificarAssento (onibus, assento);
	}
	if (opcao == 2){
		reservarAssento ();
	}
	if (opcao == 3){
		imprimirMapa (onibus, fil, pol);
	}

	return 0;
}
